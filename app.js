var catalogArray = []

function catalogItem(imageName, filePath) {
  this.imageName = imageName;
  this.filePath = filePath;
  this.tallyclicked = 0;
  this.tallyDisplayed = 0;

  imageArray.push(this);
}

new catalogItem( , )

var = randomValue = catalogArray[math.floor(math.random()*catalogArray.length)];
